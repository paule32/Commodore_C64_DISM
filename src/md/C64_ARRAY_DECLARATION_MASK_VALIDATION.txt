Validation performed in the build container
===========================================

1. Python syntax
   python -m py_compile c64c/compiler.py tests/test_c_array_declaration_mask.py
   Result: successful

2. Source-lowering regression tests with generated parser imports stubbed
   - return array[index] is retained and lowered to __d64_arr_get_1
   - array examples in comments are ignored
   - array-looking text inside literals is ignored
   - both FloodFill X arrays retain their dynamic index
   Result: 3/3 successful

3. Manual common-backend designator validation
   DesignatorExpression(GfxFloodXLow, IndexSelector(index)) resolves to the
   byte element type with a dynamic _DynamicAccess, not to the aggregate
   array type.
   Result: successful

4. Full generated-parser execution
   Not available in this container because the installed ANTLR Python runtime
   is 4.9.3 while the checked-in generated parsers require 4.13.2.
