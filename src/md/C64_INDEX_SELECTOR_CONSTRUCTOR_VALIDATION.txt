C64 IndexSelector constructor validation
========================================

Geaenderte Stelle:
    c64c/compiler.py::_array_designator()

Alt:
    IndexSelector(flat_index)

Neu:
    IndexSelector(position, flat_index)

Pruefungen:
    python -m py_compile c64c/compiler.py c64pascal/compiler.py d64_dism.py
        erfolgreich

    python -m unittest \
        tests.test_c_shift_array_operand \
        tests.test_c64_floodfill_array_limit -v
        8 Tests erfolgreich

Hinweis:
    Der vollstaendige ANTLR-Lauf war in der Erstellungsumgebung nicht moeglich,
    weil dort antlr4-python3-runtime 4.9.3 installiert ist. Die generierten
    Parserdateien des Projekts benoetigen die passende Runtime 4.13.2.
