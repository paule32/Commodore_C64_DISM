Stage 48 - Editor Mini-Map
==========================

Start program:
    d64_dism.py

New behavior:
    - Interactive mini-map to the right of the Rohdaten source editor.
    - Editor scrolling updates the mini-map viewport immediately.
    - Clicking/dragging the mini-map updates the editor's vertical scroll bar.
    - Mouse wheel over the mini-map scrolls the main editor.
    - Existing SourceTextEdit, gutter, breakpoints, bookmarks, completion and
      syntax highlighter remain in place.

Validation in this package:
    PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s tests -p 'test_*.py'

Result:
    525 tests passed.
